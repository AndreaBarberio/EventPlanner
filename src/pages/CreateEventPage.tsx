import { Container } from '@mui/material';
import CreationForm from '../components/CreationForm';

const createEventPage = () => {
	return (
		<>
			<Container className="w-12 h-30">
				<CreationForm />
			</Container>
		</>
	);
};
export default createEventPage;
